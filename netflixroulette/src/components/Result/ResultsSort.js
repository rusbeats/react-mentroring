import React from 'react';

const SearchButton = () => (
            <div>
                <button type="button" onClick="action()">SEARCH</button>
            </div>
    );
export default  SearchButton;

import React from 'react';
import { StyledNotFound } from './NotFound.styles';

// Changed to an image for '404' message
const NotFound = () => {
    return <StyledNotFound />;
};

StyledNotFound.displayName = 'NotFound';

export default NotFound;
